{{!-- <div class="row">
    <div class="col-md-12">
        <h2>Items
            <a href="/Items/add" class="btn btn-success pull-right">Add Item</a>
        </h2>
        <hr />
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Item ID</th>
                        <th>Title</th>
                        <th>ItemDate</th>
                        <th>Price</th>
                        <th>Category</th>
                        <th>Published</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {{#each items}}
                        <tr>
                            <td><a href="/item/{{this.id}}">{{this.id}}</a></td>
                            <td><a href="/item/{{this.id}}">{{this.title}}</a></td>
                            <td>{{this.postDate}}</td>
                            <td>${{this.price}}</td>
                            <td>
                                <a href="/items?category={{this.category}}">
                                    {{this.category}}
                                </a>
                            </td>
                            <td>{{#if this.published}}Yes{{else}}No{{/if}}</td>
                            <td>
                                <a href="/items/delete/{{this.id}}" class="btn btn-danger">Remove</a>
                            </td> 
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>

    <div class="row">
    <div class="col-md-12 text-center">
        {{#unless items.length}}
            <strong>{{message}}</strong>
        {{/unless}}
    </div>
</div>
</div> --}}
